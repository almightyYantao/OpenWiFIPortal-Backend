<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qunhe.its.networkportal.admin.mapper.PortalSSIDMapper">

    <select id="get" parameterType="string" resultType="com.qunhe.its.networkportal.admin.model.PortalSSIDEntry">
        SELECT * FROM portal_ssid WHERE ssid = #{ssid}
    </select>
</mapper>